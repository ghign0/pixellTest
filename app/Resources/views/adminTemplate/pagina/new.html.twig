{% extends '@adminTemplate/base.html.twig' %}

{% block content %}
    <h1>crea nuova pagina</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('pagina_index') }}">Tornaa lista delle pagine</a>
        </li>
    </ul>
{% endblock %}
